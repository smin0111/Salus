# 보안 정책

Salus는 건강정보, 알레르기, 식단 기록처럼 민감할 수 있는 정보를 다룹니다. 보안 문제는 일반 이슈보다 우선해서 확인합니다.

## 신고 방법

공개 이슈에 비밀키, 토큰, 개인정보, 실제 사용자 데이터를 올리지 마세요.

보안 문제가 의심되면 저장소 관리자에게 비공개 채널로 먼저 알려주세요. 아직 별도 보안 이메일이 없다면 GitHub 프로필 또는 저장소 소유자에게 직접 연락해 주세요.

## 민감정보 기준

다음 정보는 커밋하거나 이슈/PR에 그대로 붙이면 안 됩니다.

- `.env`, `application-secret.properties`, OAuth client secret, JWT secret
- PortOne, Gemini, Tavily, Naver 같은 외부 API 키
- 실제 사용자 이메일, 닉네임, 건강정보, 알레르기, 결제 식별자
- 운영 데이터베이스 접속 정보

## 운영 전 보안 체크

- 비밀값은 환경변수 또는 배포 플랫폼의 secret 저장소에서만 관리합니다.
- `.env.example`과 `application-secret.example.properties`에는 예시값만 둡니다.
- PR에는 인증, 결제, 개인정보, 알레르기 안전 영향 여부를 표시합니다.
- 배포 전 `mvn test`, `npm audit`, 프론트 웹 export, Docker 빌드를 확인합니다.

## 관리자 JWT 운영 기준

현재 관리자 콘솔은 별도 아이디/비밀번호 로그인이 아니라 `ADMIN` 권한 사용자의 JWT를 입력해 접속합니다. 이 방식은 기능을 작게 유지할 수 있지만, 토큰이 유출되면 만료 전까지 관리자 API를 호출할 수 있으므로 운영 규칙을 명확히 지켜야 합니다.

- 관리자 JWT는 브라우저, 메신저, 공개 이슈, PR 설명, 스크린샷에 남기지 않습니다.
- 관리자 계정은 꼭 필요한 사용자만 `ADMIN` role로 지정합니다.
- JWT 만료 시간은 짧게 유지하고, 현재 기본값은 1시간입니다.
- 관리자 권한을 부여하거나 회수한 기록은 운영 메모나 이슈가 아닌 비공개 장소에 남깁니다.
- 운영 환경에서는 `APP_ADMIN_IP_WHITELIST_ENABLED`를 켤 수 있는지 먼저 검토합니다. 단, 프록시나 컨테이너 환경에서는 백엔드가 실제 사용자 IP를 보지 못할 수 있으므로 배포 환경의 네트워크 구조를 확인한 뒤 적용합니다.
- 장기적으로는 관리자 전용 로그인, 권한 변경 이력, 토큰 강제 폐기 기능을 별도 작업으로 추가해야 합니다.

### 관리자 권한 부여/회수 절차

아직 관리자 전용 계정 관리 화면이 없으므로 운영 DB에서 `users.role` 값을 직접 변경합니다. 이 작업은 실수하면 일반 사용자가 관리자 권한을 얻을 수 있으므로 반드시 한 명의 사용자만 정확히 조회한 뒤 변경합니다.

권한을 부여하기 전 대상 사용자를 먼저 확인합니다.

```sql
SELECT id, email, role
FROM users
WHERE email = 'admin@example.com';
```

조회 결과가 정확히 한 명일 때만 관리자 권한을 부여합니다.

```sql
UPDATE users
SET role = 'ADMIN'
WHERE email = 'admin@example.com'
  AND role <> 'ADMIN';
```

관리자 권한을 회수할 때는 같은 방식으로 대상 사용자를 확인한 뒤 `USER`로 되돌립니다.

```sql
UPDATE users
SET role = 'USER'
WHERE email = 'admin@example.com'
  AND role = 'ADMIN';
```

권한 변경 후에는 새 로그인 또는 기존 JWT로 `/api/admin/dashboard/auth-check`를 호출해 결과를 확인합니다. 이 프로젝트는 요청마다 DB의 현재 `role`을 다시 읽기 때문에 `ADMIN`에서 `USER`로 회수되면 기존 JWT도 다음 요청부터 관리자 권한을 잃습니다.

## 지원 범위

현재 지원 브랜치는 `main`과 실제 배포 준비 중인 기능 브랜치입니다. 오래된 실험 브랜치는 보안 수정 대상에서 제외될 수 있습니다.
